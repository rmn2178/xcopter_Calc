# Observability (optional)
VITE_SENTRY_DSN=
VITE_SENTRY_TRACES_SAMPLE_RATE=0.05

# Data trust mode
# true  -> only provenance.status=verified rows are shown
# false -> pending rows allowed (rejected rows always hidden)
VITE_ENFORCE_VERIFIED_PRESETS=false
